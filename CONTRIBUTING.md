# 研究协作约定

## 新增选题

建议先创建 Research proposal Issue，写清楚原项目地址、想验证的能力、预期产物和许可风险。确定值得研究后，再从 `projects/_template/` 创建目录。

目录名使用小写英文和连字符，例如 `projects/local-first-sync/`。一个目录只回答一个相对独立的研究主题。

## 研究记录最低要求

每个子项目至少包含：

- 原项目与版本或提交号；
- 问题、假设和明确的不研究范围；
- 可重复的安装、运行与验证命令；
- 关键实验记录与失败尝试；
- 当前结论、局限和下一步；
- 上游许可证与必要署名。

## 提交与合并

- 分支名推荐 `study/<project-slug>`、`docs/<topic>` 或 `chore/<topic>`。
- 提交说明保持单一目的，推荐使用 `study:`、`docs:`、`demo:`、`chore:` 前缀。
- Pull Request 中勾选模板检查项，并附上能够支撑结论的验证证据。
- 在线演示必须能在纯静态 GitHub Pages 环境运行；如依赖后端，应在文档中说明替代部署方式。

## 安全与仓库体积

提交前检查凭据、个人信息、第三方数据和生成物。大型模型、数据集、视频或构建缓存不要直接进入 Git 历史；优先记录下载脚本、校验值和来源。
