# 网页验收记录

## 可复现环境

```text
Start command: python -m http.server 8043 --directory docs
Canonical local URL: http://127.0.0.1:8043/demos/h3-prompt-journal/
Browser runner: agent-browser 0.27.0
Validation date: 2026-08-21 (Asia/Shanghai)
```

截图为临时浏览器证据，保存在工具的临时目录中，没有提交到产品仓库。

## 浏览器覆盖

| 表面 | 主题 | 检查 | 结果 |
| --- | --- | --- | --- |
| 1440×900 桌面 | 浅色 | 首屏、能力地图、完整生产表单、六段预览、操作栏、其余研究内容 | pass |
| 768×1024 平板 | 深色 + reduced-motion | 双列卡、三列案例选择、表单、媒体、页面宽度 | pass |
| 390×844 手机 | 浅色 | 单列案例、单列表单、参考职责、策略参数、约束、预览 | pass |
| 1440×900 桌面 | 浅色 | 中英切换、CASE 001 三图样例、参数、声音卡与操作按钮 | pass |
| 768×1024 平板 | 深色 | CASE 004 单列信息、三图职责、原生声音和输出占位 | pass |
| 390×844 手机 | 浅色 | CASE 003 单列参数、声音卡、按钮与三图横向素材带 | pass |
| 1440×1000 桌面 | 浅色 | CASE 006 结果审计、四个下载入口、原生视频播放器 | pass |
| 768×1024 平板 | 深色 | CASE 006 单列结果卡、播放器与主题对比度 | pass |
| 390×844 手机 | 深色 | CASE 006 元数据、审计卡、下载按钮与播放器 | pass |

三个视口的 `scrollWidth - innerWidth` 均为 `-15`（垂直滚动条宽度），不存在页面级横向溢出；Prompt 预览宽度未超过工作台容器。

## 交互与状态

- 六个案例均通过真实点击切换，默认数据均显示“可复制到 H3”，并输出官方顺序的六个段落。
- CASE 001 默认输出 `3,563` 字符；段落依次为 `subject_definitions`、`summary`、`retention_analysis`、`detailed_description`、`overall_soundscape`、`non_diegetic_music`。
- 把时长改为 `20` 后显示“时长必须是 4–15 秒”并禁用复制/下载；恢复 `15` 后重新就绪。
- 关闭 CASE 004 全部五项约束后显示“至少保留一项策略约束”；恢复一项后重新就绪。
- CASE 004 选择“高潮段”后，`subject_definitions` 不再包含 Picture 3，`retention_analysis` 增加 `Picture 3: weak_reference` 的有意移除说明。
- “复制 Prompt”返回“已复制完整 Prompt，可前往 H3 粘贴并上传对应参考图”。
- “下载 .txt”返回 `已下载 h3-prompt-case-004.txt`；“恢复模板”恢复默认分段、字段和五项约束。
- 深色主题模拟后 `document.documentElement.dataset.theme === "dark"`；主题标签同步显示“浅色”。
- 首次 Tab 聚焦“跳到主要内容”，位置为页面顶部；Enter 后 URL hash 为 `#main`。
- 方向键从 CASE 001 切换到 CASE 002，再用左方向键返回 CASE 001，焦点与选中态同步。
- `prefers-reduced-motion: reduce` 浏览器模拟结果为 `true`，页面存在对应降级样式。

## 双语与真实前置信息（revision 3 历史记录）

- 页面默认进入“中文理解版”；CASE 001 的主体、场景、动作、参考职责、策略参数、声景和六段 Prompt 均为中文。
- 点击或键盘 Enter 切换 `English · H3` 后，表单和预览同步切换为英文，`aria-pressed=true`；切回中文同样通过。
- 在英文版把主体改为 `EN TEST SUBJECT` 后，切换中文仍保留中文预设；返回英文仍保留英文编辑值，证明两版独立保存。恢复模板后两版均回到对应默认值。
- CASE 002–006 的中文默认态逐一真实点击，全部显示“可复制到 H3”。
- CASE 006 英文下载反馈为 `h3-prompt-case-006.en.txt`；中文复制反馈明确写明“中文理解版”。
- 真实样例图片在浏览器报告 `naturalWidth=1254`，三个下载地址分别指向 Picture 1、`prompt.en.txt`、`prompt.zh.txt`。
- CASE 006 本地输入为 `1254×1254 / RGBA / alpha 0–255 / 890,093 bytes`；API 运行器 dry-run 验证 Prompt `3170` 字符，未发送请求、未消耗额度。
- 桌面、平板和 390px 手机的新增区域均为 `scrollWidth - innerWidth = -15`，不存在本地化导致的页面级横向溢出。
- 浏览器未报告页面错误；错误覆盖层检查为 `OK`，正文非空检查为 `HAS_CONTENT`。

## 五套带声音的真实前置信息（revision 4）

- CASE 001–005 共渲染 5 张真实样例卡、11 张原创参考图、10 个中英文 Prompt 下载按钮和 5 张 `SOUND PLAN` 卡。
- 11 张图片全部在真实浏览器中加载：横图为 `1672×941`，竖图为 `941×1672`；本地 HTTP 逐个请求均为 `200`，不存在缺图。
- 五套英文样例 Prompt 与对应案例“恢复默认”后的英文模板逐字相等；五套中文版本同样逐字相等，解决了固定下载版与上方模板偏离的问题。
- 五套英文 Prompt 均包含 `Native stereo audio`，五套中文 Prompt 均包含“原生立体声”；英文长度分别为 `3680 / 3209 / 3597 / 3791 / 3788` 字符。
- 五套均保留六段结构：`subject_definitions`、`summary`、`retention_analysis`、`detailed_description`、`overall_soundscape`、`non_diegetic_music`。
- CASE 005 英文生产版同时包含三句普通话“大家好，我今天一定一次过关！”、“稳住——还有机会！”、“我只是下来降个温。”，并明确要求对白清楚且不得重叠。
- 真实点击 CASE 001 英文下载按钮后反馈为“已下载 CASE 001 英文生产版；请同时下载卡片中的全部 Picture。”；键盘聚焦 CASE 005 英文按钮并按 Enter 也得到对应成功反馈。
- “载入上方编辑器”真实点击后，CASE 004 的案例编号、标题、默认字段和中文原生声景同步载入。
- 1440px 浅色、768px 深色和 390px 浅色均为 `scrollWidth - innerWidth = -15`；桌面三图并排，平板单列，手机三图使用局部横向素材带而不造成页面级溢出。
- 错误覆盖层为 `false`，正文长度 `7419` 字符；图片懒加载、动态 Blob Prompt 下载和输出等待状态均可用。

## CASE 006 外部真实输出（revision 5）

- 返回文件 `video_1787289612484.mp4` 已作为原始 MP4 接入页面，没有用前端动画或占位内容代替。
- `ffprobe` 报告 `10.125s / 1344×768 / H.264 / 24fps`，文件大小 `1,329,029 bytes`；音轨为 `AAC / 32kHz / 2 channels / stereo`。
- 音量扫描为平均 `-29.0 dB`、峰值 `-0.2 dB`；以 `-50 dB` 为阈值时，没有持续超过 1 秒的静音段。
- 抽帧观察到倒盐、轻敲、喂食和倒下四个节点，平面贴纸观感仍可辨认；同时如实记录盐罐旁圆形盖子仍出现的约束偏差。
- Chromium 播放器报告 `duration=10.125`、`videoWidth=1344`、`videoHeight=768`、`readyState=4`、`controls=true`；静音自动化解码测试中播放进度从 `0` 推进至 `1.162466s`。
- 页面没有自动播放，避免浏览器自动播放策略把声音验证误导为静音；播放器默认 `volume=1`、`muted=false`，声音轨存在性另由 `ffprobe` 独立验证。
- 输入图、英文 Prompt、中文 Prompt、真实 MP4 四个下载地址均经本地 HTTP 请求返回 `200`；MP4 响应类型为 `video/mp4`，长度与源文件一致。
- 1440×1000 浅色、768×1024 深色和 390×844 深色三种视口的 `scrollWidth - clientWidth` 均为 `0`；手机端播放器渲染宽度 `291px`，没有页面级横向溢出。
- 结果区同时渲染“观察通过”和“观察偏差”，浏览器错误记录为空，框架错误覆盖层为 `false`。

## 输入图持续显示（revision 6）

### 修复记录

- 当前阶段：Stage 9 · Engineering and delivery closure
- 用户目标：加入真实视频后仍保留并显示输入图，方便理解“输入 → 输出”关系。
- 修改前基线：CASE 006 真实输出区内独立 `<img>` 数量为 `0`；播放器 `poster` 直接指向 `picture-1-lemon-sticker.png`，输入证据会在播放后消失。
- 根因：输入图只被复用为视频封面，没有作为独立、持续存在的页面内容渲染。
- 最小干预：新增通用 `verified-media-pair`，把输入图和输出视频分成有标签的独立面板；从真实 MP4 的 `1.0s` 处抽取 `result-poster.jpg` 作为输出封面。
- 相邻检查：浅色桌面、深色平板、390px 深色手机、视频播放中状态、输入图链接语义、媒体 HTTP 回退。
- 决策：`pass`。

### 浏览器与媒体证据

- 修改后真实输出区存在 `1` 张独立输入图，浏览器报告 `naturalWidth=1254`、`naturalHeight=1254`、可见尺寸大于零；视频 `poster` 为 `result-poster.jpg`，不再指向输入图。
- 静音自动化播放从 `0` 推进到 `1.161209s` 时，输入图仍可见且 `opacity=1`，证明播放不会替换或隐藏输入证据。
- 1440×1000 桌面为 `172.172px + 334.781px` 并列输入输出；768×1024 平板为 `204.516px + 397.703px` 并列；390×844 手机改为两个 `291px` 面板纵向排列。
- 三个视口的页面级横向溢出均为 `0`；桌面浅色、平板深色和手机深色截图均确认标签、输入图、输出封面与播放器可读。
- 可访问树同时包含“查看 CASE 006 原始输入图”链接、四个下载入口和原生视频控制；浏览器错误记录为空，错误覆盖层为 `false`。
- 输入 PNG、输出封面 JPEG、真实 MP4 的本地 HTTP 请求均返回 `200`，响应长度分别为 `890,093 / 111,972 / 1,329,029 bytes`。

## CASE 001 三人物遮挡衔接真实输出（revision 7）

### 结果与归因证据

- 返回文件 `video_1787292798992.mp4` 已作为原始 MP4 接入；`ffprobe` 报告 `15.083333s / 1344×768 / H.264 / 24fps / 4,222,424 bytes`。
- 音轨为 `AAC / 32kHz / 2 channels / stereo`，平均响度 `-19.0 dB`、峰值 `-6.3 dB`；以 `-50 dB` 为阈值时没有持续超过 1 秒的静音段。
- 每秒抽帧确认三人的脸、发型与黑色西装 / 钴蓝夹克 / 米白连衣裙可辨认，画廊和光向基本统一，最终完成三人合影。
- `scene > 0.25` 没有发现明显硬切；较低的 `scene > 0.10` 在约 `4.0 / 4.04 / 7.04 / 10.0s` 出现变化。相邻帧检查显示这些变化与人物近身遮挡和多人构图转换对应。
- 偏差为：未来人物在遮挡完成前提前入画；第二、第三人物没有分别完成清楚的独立环绕；人物经过前景的感觉强于严格的遮挡式环绕；多人同框开始过早。
- 归因结论：提示词设计对失败有明显贡献，但模板格式不是单一根因。15 秒内堆叠三次完整环绕、两次交接和最终合影造成任务过载，且缺少站位、单向相机路径、遮挡占画比例及未来人物首次可见时间；模型面对三身份、多参考、单镜头和两次精确遮挡也存在能力方差。

### 页面与浏览器证据

- 修改前基线：`#case-001-result` 不存在；样例卡保留三张输入图，但输出槽显示“等待外部 H3 生成带声音的视频”，没有视频播放器。
- 修改后真实输出区同时存在三张输入图、真实输出封面和原生播放器；三图均加载为 `1672×941`，播放器报告 `duration=15.083333`、`videoWidth=1344`、`videoHeight=768`、`readyState=4`。
- 静音自动化播放推进至 `1.161199s` 时，三张输入图均保持可见且 `opacity=1`；视频封面为从成片 `12.0s` 抽取的 `result-poster.jpg`，不是输入图。
- 结果区同时包含“观察通过”“观察偏差”和“归因判断”；CASE 001 样例卡输出槽已改为 `REAL OUTPUT` 并回链 `#case-001-result`。
- CASE 001 样例卡仍有两个双语 Prompt 下载按钮；英文按钮触发后反馈为“已下载 CASE 001 英文生产版；请同时下载卡片中的全部 Picture。”
- 1440×1100 浅色桌面为三输入并排；768×1024 深色平板保持三输入并排；390×844 深色手机改为三输入和视频纵向排列。三个视口的页面级横向溢出均为 `0`。
- 可访问树包含三张输入图链接、四个下载入口、双语 Prompt 锚点和原生视频控制；浏览器错误记录为空，错误覆盖层为 `false`。
- 三张输入 PNG、输出封面 JPEG、真实 MP4 的本地 HTTP 请求均返回 `200`，响应长度分别为 `1,974,362 / 1,501,896 / 1,564,178 / 87,912 / 4,222,424 bytes`。

## CASE 001 三身份适用场景说明（revision 8）

- 修改前基线：`#case-001-result` 已有真实结果、偏差和归因诊断，但浏览器检查确认结果区不包含“适用场景”文字。
- 修改后新增“它的价值不是普通三人合影”说明，明确核心价值是把三张独立参考图中的三个人带入同一空间，以连续镜头建立出场顺序和人物关系。
- “适合优先尝试”包含 4 项：时尚群像 / Lookbook、影视或游戏角色预告、团队与品牌片头、MV 与多身份能力测试。
- “不建议优先使用”明确了只需最终三人合影、长对白、复杂打斗或手部互动、商业级真人精确还原及允许常规切镜等边界。
- 1440×1100 桌面端保持双栏，实测列宽约 `233px + 190px`；768×1024 平板端保持双栏，列宽约 `397px + 191px`；390×844 手机端变为 `277px` 单列。
- 三个视口的页面级横向溢出均不大于 `0`；手机截图确认列表、边界说明和后续下载按钮均未裁切。
- 回归检查仍识别 3 张 CASE 001 输入图，原生视频 `readyState=4`；浏览器错误覆盖层为 `false`，说明新增静态说明未破坏输入 / 输出对照与播放区域。

## CASE 005 水上闯关 V1 结果与 V2 修复（revision 9）

### 真实结果与失败归因

- 修改前基线：`#case-005-result` 不存在，CASE 005 样例卡输出槽为“等待外部 H3 生成带声音的视频”。
- 返回文件 `video_1787295942513.mp4` 已作为原始 MP4 接入；SHA-256 为 `C1E05DE1FFD1C2790A37B257EAE9F274637AC977B082A2F37F7319B1C5D88D2D`。
- `ffprobe` 报告 `15.083333s / 1344×768 / H.264 / 24fps / 6,714,058 bytes`；音轨为 `AAC / 32kHz / 2 channels / stereo`，平均响度 `-21.9 dB`、峰值 `-5.5 dB`，以 `-50 dB / 0.5s` 扫描没有长静音段。
- 每秒与 `7.5–14s` 每 0.25 秒抽帧显示：问候、滚筒打滑与恢复、高墙攀爬、水花和出水反应均出现；人物与黄蓝运动服基本保持。
- 较强镜头变化出现在约 `4.0 / 4.04 / 8.96 / 11.88 / 11.92s`。约 `11.9s` 的相邻画面从人物仍在爬墙直接变为水花，没有可见机关启动、接触、松手、空中轨迹和身体入水。
- 本地 faster-whisper small 中文 ASR 按顺序识别：“大家好 / 我今天一定一次过关”“稳住 还有机会”“我只是下来降个温”。主持段的 ASR 长边界包含背景声，只作为文字与顺序证据，不作为精确音节时间。
- 归因结论：V1 在 15 秒中堆叠 7 个剧情节点与 3 句对白，并广泛放权动作、机位与剪辑；“侧面机关反转”没有空间、运动、接触点和因果镜头规则。提示词与任务设计是主要贡献者，模型执行方差为次要边界。

### V2 内容与版本边界

- V2 英文生产版为 `7,161` 字符，中文理解版为 `2,620` 字符；两份均包含 `subject_definitions / summary / retention_analysis / detailed_description / overall_soundscape / non_diegetic_music` 六段和明确因果锁定。
- V2 将 7 个节点缩减为 5 段，删除独立鱼骨障碍；固定高墙、摆臂和水池几何；要求约 `9.0–11.5s` 从机关启动到身体入水采用同一条不中断侧面大全景。
- 页面明确标注 `READY TO TEST · NOT YET VERIFIED`，并说明上方视频只对应 V1；V2 不覆盖样例卡中与编辑器默认值一致的 V1。
- 两个 V2 静态下载链接均带 `download`，英文与中文文件本地 HTTP 状态均为 `200`，响应长度分别为 `7,273 / 6,726 bytes`；完整文本预览分别成功加载 `7,161 / 2,620` 字符。
- CASE 005 样例卡按钮改为“下载 V1 英文 Prompt / 下载 V1 中文 Prompt”；真实点击英文按钮后的反馈为“已下载 CASE 005 V1 英文生产版；请同时下载卡片中的全部 Picture。”

### 页面、媒体与跨表面证据

- 页面标题更新为“3 VERIFIED OUTPUTS + 3 READY INPUTS”；CASE 005 样例输出槽更新为 `REAL OUTPUT` 并回链 `#case-005-result`。
- 输入图浏览器报告 `1672×941`；播放器报告 `duration=15.083333`、`videoWidth=1344`、`videoHeight=768`、`readyState=4`，封面为成片 `10.0s` 抽帧而不是输入图。
- 静音自动化播放推进到 `5.832968s` 时，输入图仍可见且 `opacity=1`，证明播放不会隐藏输入证据。
- 1440×1100 桌面端使用顶部对齐与 `position: sticky; top: 94px`，在阅读长诊断与完整 Prompt 时持续保留输入 / 视频；768×1024 平板恢复普通单列，输入与输出仍以约 `204.516px + 397.703px` 并排；390×844 手机将媒体改为 `291px` 单列并把证据视觉顺序提前，播放器顶部约 `29px`、正文顶部约 `585px`。
- 三个视口的页面级横向溢出均不大于 `0`；手机端修复原理卡为单列。深色主题下 Prompt 面板标题使用固定 `rgb(243, 240, 230)` 浅色，修复了黑字贴近黑底的对比问题。
- 输入 PNG、输出封面 JPEG、真实 MP4、V2 英文与 V2 中文的本地 HTTP 请求均为 `200`，响应长度分别为 `1,448,582 / 145,029 / 6,714,058 / 7,273 / 6,726 bytes`。
- 浏览器错误覆盖层为 `false`；HTML parser、`node --check`、JSON 解析和 `git diff --check` 均通过。

## CASE 004 多参考帆板时尚 V1 结果（revision 10）

### 媒体、参考职责与运动学证据

- 修改前基线：`#case-004-result` 不存在，真实结果标题为“三份真实结果，三套待测输入”，CASE 004 样例卡仍显示等待外部生成。
- 返回文件 `video_1787297878588.mp4` 已作为原始 MP4 接入；SHA-256 为 `9A35887A150300E078E02D3CE5A70CA338D7CD812ABED170693EA862BF27FB5A`。
- `ffprobe` 报告 `15.083333s / 768×1344 / H.264 / 24fps / 4,850,309 bytes`；音轨为 `AAC / 32kHz / 2 channels / stereo`，平均响度 `-16.0 dB`、峰值 `-0.4 dB`，没有检测到长静音段。
- 本地 faster-whisper small 配合 VAD 没有返回任何语音段，只支持“未检测到对白或歌声”；它不能证明风、帆布、横杆、板和水声分别准确存在或同步，页面已明确这一证据边界。
- 半秒 RMS 包络从 `10.0–11.5s` 显著抬升，和 Prompt 的 `00:10` 音乐主峰值基本一致。
- 一秒抽帧与每秒四帧接触表显示：青橙运动服、深色束发、橙色帆和青色板稳定可辨；低水位、逆光、大浪和喷雾明显继承 Picture 3。人物较远，不能严谨确认脸部身份。
- 装备偏差为横杆多次形成不合理环状结构，手—杆、脚—板、身体—帆面层级不稳定；约 `8–11s` 板头出现悬浮式抬升。
- `scene > 0.12` 检测到约 `3.91667 / 9.66667 / 12.7083s` 三次明显切换，因此成片是四镜头运动时尚剪辑，不是一条连续 carve。

### 页面、播放与跨表面证据

- 页面标题更新为“4 VERIFIED OUTPUTS + 2 READY INPUTS”；CASE 004 样例卡更新为 `REAL OUTPUT`，保留三张输入图与两个 V1 双语 Prompt 下载入口，并回链 `#case-004-result`。
- 真实点击 CASE 004 的 V1 英文 Prompt 按钮后，反馈为“已下载 CASE 004 V1 英文生产版；请同时下载卡片中的全部 Picture。”，证明接入视频没有替换原有外部调度稿。
- 真实结果区独立显示三张输入图，分别标注 `IDENTITY / OUTFIT`、`RIG CONSTRUCTION`、`CAMERA LANGUAGE`；播放器封面来自成片 `13.4s` 抽帧，不是任一输入图。
- Chromium 播放器报告 `duration=15.083333`、`videoWidth=768`、`videoHeight=1344`、`readyState=4`；静音自动化播放推进时三张输入图全部保持可见。页面视频没有 `muted` 属性，验收结束后恢复 `volume=1 / muted=false`。
- 1440×1100 浅色桌面端播放器卡宽 `553px`，竖屏视频为 `360×636px`；768×1024 深色平板端三张输入图列宽约 `198.734px`，竖屏视频宽 `360px`；390×844 深色手机端三张输入图保持 `3×92px` 职责带，视频为 `291×514px`。
- 三个视口的页面级横向溢出均为 `0`；手机输入图没有被改为单列巨图，仍可同时理解三参考的角色关系。
- 三张输入 PNG、输出封面 JPEG、真实 MP4 的本地 HTTP 请求均为 `200`，响应长度分别为 `1,688,047 / 1,558,516 / 2,101,442 / 89,577 / 4,850,309 bytes`。
- 可访问树包含五个结果区下载 / 跳转链接、三张完整输入图链接和原生视频播放、音量、进度与全屏控件；浏览器错误与控制台无新增错误。
- 页面归因明确区分：参考职责架构在粗粒度视觉上有效；V1 的接触不变量欠约束；复杂装备几何与水体受力仍是模型能力边界。

## CASE 003 同一人物三姿态微型飞行 V1 结果（revision 11）

### 媒体、姿态连续性与镜头语法证据

- 修改前基线：`#case-003-result` 不存在，页面标题为“四份真实结果，两套待测输入”，CASE 003 样例卡保留三张输入图但仍显示等待外部生成。
- 返回文件 `video_1787299397694.mp4` 已作为原始 MP4 接入；SHA-256 为 `0578D3887BBC08E78172722508326C066D6A53C5583F5072C81DBC4332A2B72E`。
- `ffprobe` 报告 `15.083333s / 768×1344 / H.264 / 24fps / 4,568,037 bytes`；音轨为 `AAC / 32kHz / 2 channels / stereo`，平均响度 `-16.1 dB`、峰值 `-1.2 dB`，以 `-35 dB / 0.3s` 扫描没有静音段。
- 本地 faster-whisper small 配合 VAD 没有返回语音段，只支持“未检测到对白或歌声”；左右声道全段相关系数为 `0.986726`、差分信号比为 `0.163001`，只能说明声场统计上较窄，不能替代对气流、衣料、脚步和空间运动同步的主观听审。
- 逐秒、关键段每秒四帧和相邻帧检查确认灰黑短发、酒红外套、黑裤与同一柱廊空间稳定；三个姿态按起始 → 过渡 → 英雄顺序出现，没有身份变形、淡化、硬切或幻灯片式换页。
- 实际执行为：`0–2s` 低位进入；`2–5s` 长时间贴近鞋、腿和手；`5–9.7s` 起身经过 Picture 2；`9.7–10.4s` 转入 Picture 3；`10.4–15s` 大部分时间保持英雄姿态，只在约 `12.7s` 出现一次贴脸推拉。
- `scene > 0.12` 的变化点出现在约 `2.04 / 3.00 / 3.08 / 3.13 / 3.21 / 4.00 / 4.13 / 9.71 / 9.75 / 12.96s`；相邻帧显示它们来自连续遮挡、曝光和近身运动，不支持硬切结论。
- 微型飞行语法没有成立：缺少从靴底、衣摆、肘部间隙等可见尺度门洞穿越的镜头，也缺少连续路线、强视差、惯性和环境障碍；成片更接近低机位人体扫描与姿态巡检。

### 模板反效果诊断与版本边界

- 页面明确给出 `TEMPLATE GOAL PASSED / AESTHETIC GOAL MISALIGNED`：模板确实保住了三姿态顺序、人物身份和非幻灯片连续性，但没有因此得到更好看的动作成片。
- 三张输入本来已经是同一人物、同一服装和同一空间，继续叠加厘米级贴近、身体避让、姿态锚点、持续双运动与英雄终点，会让 15 秒任务变成检查表；人物表演被压缩成“蹲下 → 伸手 → 起身 → 举臂”。
- 这支持用户“模板版不如未套模板方案”的观感判断，但不把失败归因成“模板格式必然有害”：模板只针对避免幻灯片这一失败模式，当前真正目标若是审美与动势，应重写任务而不是完整沿用规则。
- 页面给出两个互斥的下一轮方向：若优先成片效果，以三图作为身份与动作灵感、改为一个连续舞蹈目标和三次明确镜头节拍；若专门验证微型飞行，则保留策略但加入可见尺度门槛，并把任何姿态停留限制在 `0.4s` 内。
- 本次没有伪造 V2 成功或覆盖 V1。样例卡仍提供与当时生成一致的 V1 中英双语 Prompt，修复方向只作为下一轮实验设计。

### 页面、播放与跨表面证据

- 页面标题更新为“五份真实结果，一套待测输入”；CASE 003 样例卡更新为 `REAL OUTPUT`，保留三张姿态图、两个 V1 双语 Prompt 下载按钮并回链 `#case-003-result`。
- 真实点击 V1 英文 Prompt 按钮下载文件名为 `h3-real-sample-case-003.en.txt`，反馈为“已下载 CASE 003 V1 英文生产版；请同时下载卡片中的全部 Picture。”
- 真实结果区持续显示三张输入图并分别标注 `START POSE / WAYPOINT / HERO POSE`；播放器封面来自成片 `8.4s` 抽帧，不是输入图。
- Chromium 播放器报告 `duration=15.083333`、`videoWidth=768`、`videoHeight=1344`、`readyState=4`；视频没有 `muted` 属性且 `defaultMuted=false`。手机端静音自动化播放推进至 `0.991485s` 时三张输入图仍全部存在，验收结束后恢复非静音状态。
- 1440×1100 浅色桌面三张输入列宽约 `171px`，竖屏视频宽 `358px`；768×1024 深色平板三列约 `204px`，视频宽 `358px`；390×844 深色手机三列约 `97px`，视频宽 `304px`。三个视口页面级横向溢出均为 `0`。
- 手机端相邻 CASE 004 仍保留三列输入图，列宽约 `97px`，播放器 `readyState=4`，证明共享响应式选择器没有破坏已有案例。
- 结果区包含 8 个可操作链接、3 张带完整替代文本的输入图、正确的 `aria-labelledby` 和原生视频控件；样例反馈使用 `aria-live="polite"`。三个视口均无控制台错误、页面错误或错误覆盖层。
- 三张输入 PNG、输出封面 JPEG、真实 MP4 的本地 HTTP 请求均为 `200`，响应长度分别为 `1,841,060 / 1,716,579 / 1,818,770 / 79,532 / 4,568,037 bytes`。

## 媒体与回退

- 固定提交的视频成功加载元数据，浏览器报告时长 `36.152993s`。
- 页面明确记录 README 的 45 秒描述与附件约 36 秒之间的差异。
- 视频使用 `preload="metadata"`；旁边保留固定提交的直接链接，作为媒体失败时的可操作回退。

## 性能观察

本地静态服务的浏览器观测：

| 指标 | 结果 |
| --- | --- |
| TTFB | 1.9ms |
| FCP | 32ms |
| LCP | 32ms（h1） |
| CLS | 0 |

这些数值只证明本地静态页面的主要内容没有被外部视频阻塞，不代表 GitHub Pages 的公网性能保证。

## 工程检查

- `node --check docs/demos/h3-prompt-journal/app.js`：pass。
- Python 标准库 HTML parser：两个 Pages HTML 均可解析。
- 必需交付文件：全部存在。
- `git diff --check`：pass。
- 浏览器错误与控制台：未报告页面错误。
- 总展厅“进入实验台”链接：成功导航到 `/demos/h3-prompt-journal/`。

## 验收中修复的问题

1. `100vw` 把滚动条宽度算入深色全宽区，桌面产生 8px 横向溢出；改为只扩展绘制区域，复验通过。
2. 五张能力卡在平板双列布局中留下空格；最后一张改为占满整行，移动端仍为单列。
3. 页面最初沿用 README 的 45 秒说法；读取真实媒体元数据后改为明确标注附件约 36 秒及其证据差异。
4. 第一版生产表单只监听 `input` 事件，自动化选择 CASE 004 高潮段时界面值变化但生成状态未更新；补充 `change` 事件后，画幅和分段选择均即时重建 Prompt。

## 终审

设计契约中的生产工作台必需行已完成本地验收；远端 Pages 发布状态在对应 PR 和工作流中另行核验。
